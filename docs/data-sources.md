# 数据源与接口约定

本文记录当前项目使用或计划使用的公开交通接口，以及实现时需要注意的字段语义。

## 九巴 KMB

基础地址：

```text
https://data.etabus.gov.hk/v1/transport/kmb
```

常用接口：

```text
路线列表：
GET /route/

路线详情：
GET /route/{route}/{bound}/{service_type}

路线站点：
GET /route-stop/{route}/{bound}/{service_type}

站点详情：
GET /stop/{stop_id}

到站时间：
GET /eta/{stop_id}/{route}/{service_type}
```

完整 ETA URL 模板：

```text
https://data.etabus.gov.hk/v1/transport/kmb/eta/{stop_id}/{route}/{service_type}
```

### 关键字段

| 字段 | 含义 |
| --- | --- |
| `eta` | 预计到站时间，ISO 8601 字符串 |
| `data_timestamp` | 该 ETA 数据更新时间 |
| `eta_seq` | 同一路线同站点返回的第几班 |
| `service_type` | 九巴服务类型，同一路线可能有多种走线/时段 |
| `seq` | 该站在当前 service type 下的站序 |
| `dest_sc` / `dest_tc` / `dest_en` | 官方目的地显示 |
| `rmk_sc` / `rmk_tc` / `rmk_en` | 备注，例如“原定班次” |

### 实时与原定班次

九巴 ETA 可能同时返回实时预计和预定班次。当前页面规则：

- `rmk_sc` 或 `rmk_tc` 为空：显示为“实时预计”
- `rmk_sc` 或 `rmk_tc` 非空：显示原始备注，例如“原定班次”

这不是项目自行计算的巴士位置，而是九巴官方 ETA 接口提供的到站信息。

### 272A 已实现站点配置

已确认站点：

| 站点 | stop_id | 经纬度 | 页面显示方向 |
| --- | --- | --- | --- |
| 逸瓏灣 / 逸珑湾 `PA150` | `5747450359C7907C` | `22.431675, 114.203753` | `往大学站方向` |
| 雲滙 / 云汇 `PA206` | `3F24CFF9046300D9` | `22.429805, 114.203323` | `往大学站方向` |
| 大學站 / 大学站 `ST905` | `9F542D4B6CF41651` | `22.412678, 114.210665` | `往白石角方向` |

共同配置：

| 项目 | 值 |
| --- | --- |
| 路线 | `272A` |
| 营运商 | `KMB` |

白石角离开方向查询的 `service_type` 需要按站点精确配置：

| 站点 | service_type |
| --- | --- |
| 逸珑湾 `PA150` | `1,2,3,4` |
| 云汇 `PA206` | `1,3,4` |

原因：`service_type=2` 是大学站开出的凌晨短程，只到博研路，不回大学站；`PA206` 在博研路之后的回程段，所以不应把 `2` 当作云汇往大学站方向的有效班次。

各 `service_type` 含义：

| service_type | 含义 |
| --- | --- |
| `1` | 主循环线 |
| `2` | 凌晨短程至博研路，不回大学站 |
| `3` | 清晨白石角往大学站 |
| `4` | 经科技大道西 |

大学站往白石角方向查询的 `service_type`：

| service_type | 含义 |
| --- | --- |
| `1` | 主循环线 |
| `2` | 凌晨短程至博研路 |
| `4` | 经科技大道西 |

注意：九巴可能对同一班车在多个 `service_type` 返回相同 `eta`，但目的地字段不同。因此页面按路线和 ETA 时间窗口去重，不把目的地字段作为去重条件。

## 绿色专线小巴 GMB

基础地址：

```text
https://data.etagmb.gov.hk
```

常用接口：

```text
路线/班次：
GET /route/NT/{route_code}

路线站点：
GET /route-stop/{route_id}/{route_seq}

到站时间：
GET /eta/route-stop/{route_id}/{route_seq}/{stop_seq}
```

小巴实现注意：

- 同一路线代码可能对应多个 `route_id`
- 必须明确 `route_id`、`route_seq`、`stop_seq`
- ETA 可能返回 `enabled=true` 但 `eta=[]`，表示端点有效但当前没有 ETA
- 小巴官方站名与九巴站名不完全一致；页面显示使用九巴站名，内部配置保留小巴官方站点信息
- 小巴 API 当前未返回 `Access-Control-Allow-Origin`，浏览器不能直接跨域请求；页面通过 Cloudflare Pages Function 的同源 `/api/gmb/...` 代理访问

### 27A 已实现站点配置

当前只接入 27A 普通走线 `route_id=2007861`。

已实现方向：

| route_seq | 方向 |
| --- | --- |
| `1` | 白石角(天赋海湾) → 沙田(排头街) |
| `2` | 沙田(排头街) → 白石角(天赋海湾) |

页面与小巴官方站点映射：

| 页面显示 | 方向 | route_seq | 小巴官方站名 | stop_id | stop_seq |
| --- | --- | --- | --- | --- | --- |
| 逸珑湾 `PA150` | 往沙田 | `1` | 科进路停车湾近DC2431灯柱 | `20007310` | `4` |
| 云汇 `PA206` | 往沙田 | `1` | 创新路, 近云滙 | `20007094` | `5` |
| 沙田站 `排头街` | 往白石角 | `2` | 沙田中, 排头街, 港铁沙田站外 | `20015839` | `1` |

暂缓接入的 27A 变体：

| route_id | 官方描述 | 当前处理 |
| --- | --- | --- |
| `2007862` | 辅助服务，经科技大道西 | 暂不接入，需另行确认站点映射 |
| `2007893` | 特别班次，经海日湾 | 暂不接入，需另行确认站点映射 |
| `2007894` | 特别班次，经海日湾及创新路 | 暂不接入，需另行确认站点映射 |

暂缓原因：这些变体不是“同站不同班次”的简单补充，而是走线和站序会变化。若直接合并，可能把并非同一等车位置的 ETA 显示到同一个站点卡片里。

小巴 ETA 字段：

| 字段 | 含义 |
| --- | --- |
| `timestamp` | 预计到站时间 |
| `diff` | 距离到站分钟数 |
| `remarks_sc` / `remarks_tc` / `remarks_en` | 备注，例如“未开出” |

页面显示规则与九巴一致：备注为空时显示“实时预计”，备注存在时显示官方备注。

### 27A 待接入变体的候选站点映射

未来接入 `2007893` / `2007894` 海日湾特别班次时，先按以下映射核对：

| 页面显示 | route_id | 方向 | 小巴官方站名 | stop_id | stop_seq | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 逸珑湾 `PA150` | `2007893` / `2007894` | 往沙田 | 科进路停车湾近DC2431灯柱 | `20007310` | `3` | 与当前 27A 普通线的逸珑湾位置一致 |
| 云汇 `PA206` | `2007893` / `2007894` | 往沙田 | 创新路南行巴士停车湾近CE2135灯柱 | `20015873` | `6` | 用户现场确认与 PA206 基本在一起 |
| 云汇相邻站 | `2007893` / `2007894` | 往沙田 | 科进路西行云汇外停车湾近DE2448灯柱 | `20015872` | `5` | 同一特别班次也停靠，但不默认作为 PA206 主映射 |

27A 往沙田页面标注：

| route_id | 页面标注 |
| --- | --- |
| `2007861` | 快车 |
| `2007862` | 快车 |
| `2007893` | 慢车 |
| `2007894` | 慢车 |

27A 往白石角页面标注与沙田站映射：

| 页面显示 | route_id | 方向 | 小巴官方站名 | stop_id | stop_seq | 页面标注 |
| --- | --- | --- | --- | --- | --- | --- |
| 沙田站 `排头街` | `2007861` | 往白石角 | 沙田中, 排头街, 港铁沙田站外 | `20015839` | `1` | 快车 |
| 沙田站 `排头街` | `2007862` | 往白石角 | 沙田中, 排头街, 港铁沙田站外 | `20015839` | `1` | 快车 |
| 沙田站 `排头街` | `2007893` | 往白石角 | 沙田中,排头街, 港铁沙田站外 | `20015880` | `1` | 慢车 |
| 沙田站 `排头街` | `2007894` | 往白石角 | 沙田中,排头街, 港铁沙田站外 | `20015880` | `1` | 慢车 |

### 27B 已实现站点配置

当前接入 27B `route_id=2007895`，页面统一标注 `经大学站`：

| 页面显示 | 方向 | route_seq | 小巴官方站名 | stop_id | stop_seq | 页面标注 |
| --- | --- | --- | --- | --- | --- | --- |
| 逸珑湾 `PA150` | 往沙田 | `1` | 科进路停车湾近DC2431灯柱 | `20007310` | `4` | 经大学站 |
| 云汇 `PA206` | 往沙田 | `1` | 创新路, 近云滙 | `20007094` | `5` | 经大学站 |
| 大学站 `ST905` | 往白石角 | `2` | 港铁大学站巴士总站 | `20002275` | `2` | 经大学站 |
| 沙田站 `排头街` | 往白石角 | `2` | 沙田中, 排头街, 港铁沙田站外 | `20015839` | `1` | 经大学站 |

## 港铁 MTR

基础地址：

```text
https://rt.data.gov.hk/v1/transport/mtr
```

东铁线实时到站：

```text
GET /getSchedule.php?line=EAL&sta={station_code}
```

当前换乘推荐使用：

| 站点 | station_code | 使用方向 |
| --- | --- | --- |
| 沙田站 | `SHT` | `UP`，往罗湖/落马洲方向 |
| 大学站 | `UNI` | `UP`，往罗湖/落马洲方向 |

港铁接口返回 `Access-Control-Allow-Origin: *`，可由浏览器直接请求。

换乘推荐规则：

1. 根据用户手动选择的东铁线位置，取可考虑的换乘站：沙田站、大学站，或仅大学站。
2. 取该站下一班东铁北行列车到站时间；如果用户选择“已到沙田”或“已到大学”，对应站点到站时间视为当前时间。
3. 对每个换乘站，只考虑 `巴士/小巴 ETA >= 东铁到站时间 + 5 分钟` 的班次。
4. 推荐换乘等待时间最短的方案；等待时间按向上取整显示。

为了避免把已经越过沙田、用户赶不上的大学站列车当作候选，页面会对大学站到达时间做下限校正：沙田到大学至少按约 4 分钟，沙田与大学之间至少按约 2 分钟。

## 本地参考资料

项目内已有两份路线资料：

- `pak-shek-kok-transit-notes.md`
- `pak-shek-kok-transit-routes.json`

新增路线前，优先从这两份资料找站点和方向，再用官方 API 实测核对。
