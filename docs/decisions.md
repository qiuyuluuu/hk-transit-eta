# 决策记录

本文记录已经确认过的产品和技术决定，避免后续反复讨论。

## 2026-06-29：第一版使用纯静态网页

决定：先做 `index.html + app.js + styles.css` 的纯前端页面。

原因：

- 当前只查询少量站点/路线
- 九巴 API 支持浏览器跨域访问
- iPhone 可以通过 Safari 添加到主屏幕
- 不需要为第一版引入后端、部署密钥或数据库

后续如果加入通知、缓存或多营运商复杂聚合，再考虑 Cloudflare Worker。

## 2026-06-29：界面使用简体中文

决定：用户界面显示简体中文。

说明：

- 官方 API 同时提供繁体、简体、英文
- 本地路线资料可以保留官方繁体站名
- 面向用户的页面使用简体，例如“逸珑湾”“往大学站方向”

## 2026-06-29：272A 逸珑湾显示为“往大学站方向”

决定：页面显示方向为“往大学站方向”。

原因：

- 九巴官方目的地对循环线常显示为“白石角(循环线)”
- 用户在逸珑湾站点查看时，关心的是车是否快到并继续往大学站方向走
- 生活化方向名比官方循环线目的地更可读

## 2026-06-30：站点标题显示站点编码

决定：站点名称旁直接显示九巴站点编码，例如 `逸珑湾 PA150`、`云汇 PA206`。

原因：

- 272A 存在同名或近似站点，例如 `云滙 (PA125)` 和 `云滙 (PA206)`
- 编码可以帮助确认用户看到的是哪一个物理站点
- 后续新增站点时，编码比纯站名更适合作为界面和文档的稳定标识

## 2026-06-30：云汇默认加入 PA206

决定：本轮加入的是 `云汇 PA206`，方向同样显示为“往大学站方向”。

说明：

- 272A 主循环线中也有 `云滙 PA125`
- `PA206` 是经过逸珑湾和博研路之后、继续往大学站方向的云汇站点
- 如果需要查看另一侧的 `PA125`，后续作为独立站点加入

## 2026-06-30：页面按通勤场景分组

决定：页面分成两个通勤场景：

- `上班`：`往大学、沙田方向`
- `下班`：`往白石角方向`

原因：

- 用户实际使用时更关心“现在是在离开白石角，还是回到白石角”
- 272A 是循环线，官方目的地字段不总是贴合用户语境
- 分组比按路线机械排列更适合手机上快速扫一眼

## 2026-06-30：加入大学站 ST905 回白石角

决定：在下班分组加入 `272A / 大学站 ST905 / 往白石角方向`。

查询 `service_type=1,2,4`。不默认查询 `service_type=3`，因为 `3` 是清晨白石角往大学站的服务，不是大学站开出。

## 2026-06-30：27A 站名用九巴标签、内部保留小巴站点

决定：27A 页面显示沿用九巴站点名和编码，例如 `逸珑湾 PA150`、`云汇 PA206`，但内部使用小巴官方 `route_id / route_seq / stop_seq` 查询 ETA。

历史状态：最初只加入 27A 普通走线 `route_id=2007861` 的往沙田方向：

- `逸珑湾 PA150` → 小巴 `科进路停车湾近DC2431灯柱`，`stop_seq=4`
- `云汇 PA206` → 小巴 `创新路, 近云滙`，`stop_seq=5`

回白石角方向最初也使用同一普通走线：

- `沙田站 排头街` → 小巴 `沙田中, 排头街, 港铁沙田站外`，`route_seq=2`，`stop_seq=1`

原因：

- 小巴系统站名多使用道路、屋苑、灯柱描述，和九巴站牌名不同
- 用户实际识别位置时更熟悉当前页面已使用的九巴站名
- route_id 变体很多，先接入普通走线，特殊走线后续单独确认

当前状态：上述“只接入普通走线”已被后续 `27A 往沙田变体按快车/慢车标注` 和 `27A 往白石角变体按快车/慢车标注` 两项决策取代。保留下来是为了记录为什么一开始没有直接合并所有变体。

## 2026-06-30：小巴 ETA 通过同源代理访问

决定：绿色专线小巴 ETA 不由浏览器直接请求，而是通过 Cloudflare Pages Function 暴露同源 `/api/gmb/eta/route-stop/...`。

原因：

- 小巴 API 当前响应头没有 `Access-Control-Allow-Origin`
- 浏览器会因 CORS 拦截直连请求，页面显示“更新失败”
- 服务器端代理可以正常请求小巴 API，再把 JSON 返回给页面

## 2026-06-30：早期暂缓完整 27A

决定：27A 先只接入普通走线 `route_id=2007861`，暂不把 `2007862 / 2007893 / 2007894` 合并进页面。

状态：已被后续决策取代。当前页面已经接入 `2007861 / 2007862 / 2007893 / 2007894`，并以 `快车` / `慢车` 标签区分。本文保留这一节作为早期风险评估记录。

暂缓的变体：

- `2007862`：辅助服务，经科技大道西
- `2007893`：特别班次，经海日湾
- `2007894`：特别班次，经海日湾及创新路

原因：

- 这些变体的走线和站序会变化，不是简单的同站不同班次
- `逸珑湾 / 云汇` 在不同变体里可能对应不同小巴官方站点
- 直接合并可能显示并非同一等车位置的 ETA，误导现场判断
- 当前优先目标是把普通走线和小巴代理在 iPhone 上跑稳

触发后续补全的条件：

- 部署试用后发现页面无车但现场有 27A
- 某些时段经常缺少 27A ETA
- 用户明确需要经科技大道西、经海日湾或经海日湾及创新路的班次

## 2026-06-29：272A 查询 service_type 1, 2, 3, 4 并去重

决定：已实现的 `272A` 站点按物理站点和方向精确配置 `service_type`，再按 ETA 时间窗口去重。

当前配置：

- `逸珑湾 PA150`：`1,2,3,4`
- `云汇 PA206`：`1,3,4`
- `大学站 ST905`：`1,2,4`

原因：

- 四种 service type 都经过 `逸瓏灣 (PA150)`
- `service_type=2` 是大学站至博研路短程，不回大学站，因此不适合 `云汇 PA206` 往大学站方向
- `service_type=3` 是清晨白石角往大学站，因此不适合大学站开出的回白石角方向
- 官方可能对同一班车在多个 service type 返回相同 ETA
- 同一 ETA 可能带不同目的地字段，例如“大学站”“博研路”“白石角(循环线)”
- 因此去重不依赖目的地字段，只按同一路线和接近的 ETA 时间判断

当前时间窗口：90 秒。

## 2026-06-30：27A 海日湾变体的 PA206 映射

决定：未来如果接入 27A `2007893` / `2007894` 往沙田方向的海日湾特别班次，`云汇 PA206` 这一页面站点优先映射到小巴官方站点 `创新路南行巴士停车湾近CE2135灯柱`，即 `stop_id=20015873`、`stop_seq=6`。

说明：
- 用户现场确认 CE2135 灯柱站与九巴 `PA206` 基本在一起，比单纯按“云汇”字样匹配更贴近实际候车位置。
- 同一批海日湾特别班次还会停 `科进路西行云汇外停车湾近DE2448灯柱`，即 `stop_id=20015872`、`stop_seq=5`；该站可作为相邻站点记录，但不默认作为 `PA206` 页面卡片的主映射。
- 逸珑湾 `PA150` 在这些变体中仍可映射到 `科进路停车湾近DC2431灯柱`，即 `stop_id=20007310`、`stop_seq=3`。

## 2026-06-30：27A 往沙田变体按快车/慢车标注

决定：27A 往沙田方向不按变体拆成多张卡片；同一站点仍只显示一张 `27A` 卡片，在每个预计时间旁以小标签标注 `快车` 或 `慢车`。

规则：
- `快车`：`route_id=2007861`、`route_id=2007862`
- `慢车`：`route_id=2007893`、`route_id=2007894`

原因：用户主要关心到沙田的用时判断，不关心具体绕行哪里。`快车/慢车` 是产品语义，不宣称官方全程行车时间；页面仍显示官方 ETA，并用颜色辅助区分。

## 2026-06-30：27A 往白石角变体按快车/慢车标注

决定：27A 往白石角方向沿用同一思路，不按变体拆卡；沙田站 `排头街` 卡片合并查询四个 `route_id`，并在每个 ETA 旁标注 `快车` 或 `慢车`。

规则：
- `快车`：`route_id=2007861`、`route_id=2007862`
- `慢车`：`route_id=2007893`、`route_id=2007894`

站点映射：
- `2007861` / `2007862`：沙田站使用 `stop_id=20015839`、`route_seq=2`、`stop_seq=1`
- `2007893` / `2007894`：沙田站使用 `stop_id=20015880`、`route_seq=2`、`stop_seq=1`

说明：两个官方 `stop_id` 的名称都对应排头街港铁沙田站外；页面保持用户熟悉的 `沙田站 排头街` 标签。

## 2026-06-30：加入 27B 大学站往白石角

决定：在下班组加入 `27B / 大学站 ST905 / 往白石角方向`，内部查询小巴 `route_id=2007895`、`route_seq=2`、`stop_seq=2`、`stop_id=20002275`。

显示规则：该项不标注 `快车` / `慢车`，而是在 ETA 旁标注 `经大学站`。页面站点编码沿用 `ST905`，使其和同一物理场景下的 `272A 大学站 ST905` 排在一起。若 27B 返回空 ETA，页面隐藏对应 27B 卡片，避免无班次时占用空间。

## 2026-06-30：补全 27B 常用站点

决定：继续加入 27B `route_id=2007895` 的三个常用查询项：

- `逸珑湾 PA150 / 往沙田方向`：`route_seq=1`、`stop_seq=4`、`stop_id=20007310`
- `云汇 PA206 / 往沙田方向`：`route_seq=1`、`stop_seq=5`、`stop_id=20007094`
- `沙田站 排头街 / 往白石角方向`：`route_seq=2`、`stop_seq=1`、`stop_id=20015839`

显示规则与 27B 大学站项一致：不使用 `快车` / `慢车`，统一标注 `经大学站`；若返回空 ETA，隐藏对应 27B 卡片。

## 2026-06-30：加入东铁线换乘推荐

决定：在下班组顶部加入换乘推荐。用户手动选择当前位置，页面结合港铁东铁线实时到站、沙田站/大学站的巴士与小巴 ETA，推荐在沙田或大学下车换乘。

位置选项：
- `已到大围站`：比较沙田站与大学站
- `已到沙田站`：沙田站到站时间视为当前时间，同时仍比较大学站

规则：
- 按换乘站分别预留到换乘点时间：沙田站 4 分钟，大学站 3 分钟
- 只考虑 `巴士/小巴 ETA >= 东铁到站时间 + 该站换乘时间` 的候选
- 推荐换乘等待时间最短的方案
- 等待时间向上取整显示
- 到站时间会做下限校正：大围到沙田至少约 3 分钟，沙田到大学至少约 7 分钟，避免使用用户赶不上的列车
- 若港铁实时到站 API 暂不可用，页面按同一组时间估算并继续推荐：大围到沙田 3 分钟、沙田到大学 7 分钟
- 换乘推荐标题栏右侧显示港铁状态框：绿色 `港铁实时可用` 或红色 `港铁实时不可用，已估算`

原因：GPS 在列车与站内环境下可能漂移，第一版用手动位置选择更稳定；港铁东铁线实时到站 API 可直接跨域访问，适合先做轻量推荐。

补充原因：港铁实时 API 偶发不可用时，用户仍需要一个保守但可行动的换乘判断；用固定行车时间兜底比直接显示“推荐暂不可用”更符合通勤场景。

实现说明：换乘时间保存在 `app.js` 的 `transferStations` 配置表中，后续按站点调整时只改配置值。

## 2026-06-30：上班/下班改为页面切换

决定：上班和下班不再纵向连续展示，而是用顶部滑块切换成两个页面。默认显示上班页，刷新仍会更新全部 ETA 缓存，切换页面时不重新请求接口。

原因：
- 手机首屏更紧凑，减少滚动
- 用户当前只会处在一个通勤场景中，滑块切换比同时展示两组信息更明确
- 保留全部数据刷新可以避免切到另一页后还要等待

## 2026-06-30：加入上班出门建议

决定：在上班组顶部加入“出门建议”。页面复用上班组已刷新出的 ETA，在预留 5 分钟步行/候车时间的前提下，显示最早能乘上的两班。

规则：
- 候选范围只包含上班组现有卡片，不额外请求新接口
- 只考虑 `ETA >= 当前时间 + 5 分钟` 的班次
- 按 ETA 从早到晚排序，取前两项
- 建议出门时间为 `ETA - 5 分钟`
- 同一分钟的不同路线、站点或 27A 快慢车都保留为独立候选

原因：用户早上最关心“现在最早应该什么时候出门、去哪个站点、坐哪一班”。复用现有 ETA 可以保持推荐与卡片显示一致，也避免重复请求 API。

## 2026-06-29：备注为空显示“实时预计”

决定：当九巴 `rmk_sc` / `rmk_tc` 为空时，页面显示“实时预计”；当备注不为空时显示官方备注。

原因：

- 九巴会返回“原定班次”等备注
- 用户需要区分更可信的实时 ETA 和只按时刻表推算的班次
- 页面不自行计算巴士位置，只展示官方 ETA 与备注语义
